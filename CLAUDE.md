# 이 저장소에서 일하는 방법

한국 주식에서 **거래비용을 넘는 롱온리 우위가 있는지** 검정하는 도구다. 전략을 만들어
성과를 자랑하는 프로젝트가 아니라 **그럴듯해 보이는 신호를 기각하는** 프로젝트다.
12개 가설을 검정해 12개를 기각했고, 배포 가능한 전략은 0개다. 그게 정직한 결과다.
**PEAD는 네 번 구현해 네 번 실패하고 닫았다** — 사전 등록에 "5차는 없다"고 적었으니
다시 열지 않는다.

**이벤트 축은 검정력부터 재고 들어간다.** 보유형(그 일이 일어난 종목을 산다)은
종목이 3~9개뿐이라 연 11~12% 초과가 있어야 검출된다 — 그런 이벤트 효과는 없다.
쓸 수 있는 것은 제외형뿐이고, 그 산술은 `초과 = 제외비중/(1−제외비중) × 부진폭`이다.

**제외형은 두 번 검정해서 두 번 다 같은 자리에서 막혔다**(유상증자 t 1.42, 순 발행
t 1.12). 고르는 데는 성공했는데(바스켓 −3.22%, −4.01%) 크기가 모자란다. 통과하려면
부진폭 6~7%가 필요하다. **그리고 무작위 위약으로 잰 추적오차는 하한이다** — 실제
제외 바스켓은 서로 닮아서 분산이 안 되고, 실측이 무작위의 두 배(2.07% vs 1.13%)였다.

수치와 판단 근거는 전부 [`experiments/log.md`](experiments/log.md)에 시간순으로 있다.
**새 작업을 시작하기 전에 마지막 3~4개 항목을 읽을 것.**

## 반드시 지킬 것

이 규율들은 하나하나 실제로 깨져서 잘못된 결론을 낸 적이 있어서 생겼다.

**1. 가설은 보기 전에 등록한다.** 측정 방식·판정 기준·표본 구간을 스크립트 docstring에
먼저 적고 **한 번만** 돌린다. 이 저장소는 이미 40건 넘게 검정해서, 우연히 t 3이 나오는
것이 정상인 상태다. 여러 변형을 돌려보고 좋은 것을 고르면 반드시 뭔가 나온다.

**2. 결과를 본 뒤 떠올린 변형은 새 가설이다.** 같은 현상을 두 번째로 보면 Bonferroni를
적용한다(2건 → \|t\| > 2.24, 3건 → 2.39). "유니버스를 넓히면 될 것 같다"는 생각이
들면 그 순간 카운터가 하나 올라간다.

**3. 반증 확인을 같이 등록한다.** "이 신호가 진짜라면 반대쪽은 반대 부호여야 한다"를
미리 정해두고 본다. 이게 자사주 가설을 살렸다 — t 2.14로 통과할 뻔했는데, 취득과
처분이 **둘 다** 양수여서 고유분이 t 0.56임이 드러났다.

**4. 롱온리에서는 IC가 아니라 상위 분위 초과를 본다.** 예측력이 하위 구간에 몰려
있으면 IC는 좋아도 살 수 있는 게 없다. 저변동성(IC t 3.14 / 롱온리 t −0.13)과
밸류(IC t 2.95 / 롱온리 t 1.33)가 정확히 그랬다. **한국 대형주에서 횡단면 예측력은
하위 구간에 몰려 있고, 롱온리는 그 정보의 60% 이상을 버린다.** 이게 이 프로젝트에서
얻은 가장 견고한 사실이다.

**5. 벤치마크는 전략의 후보 집합으로 잡는다.** 전략이 고르는 후보 집합이 유니버스와
다르면, 유니버스를 기준으로 삼는 순간 '후보에 든 것만으로 생기는 차이'가 전략 성과로
잡힌다. `Strategy.benchmark()`로 전략이 자기 기준을 지정한다.

**6. 숫자가 안 맞으면 그냥 넘어가지 않는다.** 이 저장소에서 나온 가장 큰 t(4.12)는
버그였고, 그걸 잡은 건 새 검정이 아니라 "1,439종목 요청했는데 848개가 왔다"를
파고든 것이었다.

**7. 유의하지 않다고 "없다"로 적지 않는다.** 검정력을 먼저 재고(`run_power_check.py`),
표본이 볼 수 있는 크기인지 구분해서 기록한다. 미국 모멘텀조차 5년 구간에서 37%만
검출된다.

## 이미 밟은 함정

같은 곳을 다시 밟지 말 것. 전부 회귀 테스트가 붙어 있다.

| 함정 | 증상 |
|---|---|
| **DART 배치 캐시를 순번으로 이름** | 유니버스를 넓히자 591종목 재무가 조용히 빔 → t 4.12짜리 가짜 신호 |
| **수집기와 분석의 유니버스 정의 불일치** | 목록이 1,235 vs 1,439로 갈려 분석 중 네트워크 호출 |
| **`shift(4)`가 "4행 전"** | DART는 분기가 자주 빔(400종목 전부, 중앙값 5) → 전년 동기가 아닌 것과 비교 |
| **같은 분기 중복 행 2,844개** | `sort_values`가 불안정 정렬 → 데이터를 더 받으면 **과거 SUE가 바뀜** |
| **DART Q4는 연간 누적** | Q1~Q3는 3개월인데 사업보고서는 1년치. TTM은 Q4를 되돌려야 함 |
| **조정 종가 × 원본 거래량** | 삼성전자 거래대금이 최대 52배 → 시장충격 7분의 1로 과소평가 |
| **기업행위 보정 문턱에 가격제한폭을 씀** | "원본 30% 급변"이 조건이라 무상증자 20%(주가 −16.7%)를 놓침 → 미보정 325건, 313건이 가짜 하락 |
| **시장충격 계수에 변동성 항 없음** | 상수 0.1이 "Y × 변동성"을 흡수 → 판정이 그 값에 갈림 |
| **참여율 0으로 나누기** | 거래정지 종목에서 백테스트 전체가 조용히 −inf |
| **`dates[::20]` 리밸런싱 격자** | 패널 시작점에 따라 날짜가 밀림 → 백테스트와 운용이 다른 날을 말함 |
| **pykrx import 시 KRX 로그인 시도** | 이용약관 위반으로 계정 차단됨. **절대 pykrx로 스크래핑하지 말 것** |

## 데이터

| 종류 | 출처 | 범위 |
|---|---|---|
| 전종목 시세·시총 | KRX Open API (인증키 + 서비스 승인) | 2010-01-04 ~ 2026-08-26 |
| 분기 재무제표 | DART OpenAPI | 2015 ~ 2026, 43,178행 |
| 주요사항보고서 공시목록 | DART `list.json` | 2015-01 ~ 2026-08, 84,511건 |
| 검증용 팩터 수익률 | Ken French | 1926~ (미국) |

> **KRX 접근**: 초기에 pykrx로 `data.krx.co.kr`을 스크래핑하다 차단당했다. 지금은
> Open API만 쓴다. `src/data_loader/env.py`가 import 전에 KRX 자격증명을 지운다.
> KRX API는 특정 네트워크에서만 열린다(사용자가 와이파이를 바꿔야 함). DART는 별개
> 호스트라 항상 된다.

API 키는 루트의 `*.env`에 두고 git에 올리지 않는다: `KRX_API_KEY=`, `DART_API_KEY=`.
`data/`는 재생성 가능해서 git에 없다.

## 명령

```bash
python -m pytest tests/ -q
```

```bash
python scripts/run_backtest.py --top-k 500
```

```bash
python scripts/run_today.py --capital 3000
```

수집(중단해도 이어받음):

```bash
python scripts/collect_openapi_panel.py
```

```bash
python scripts/collect_dart_fundamentals.py 2015 2026 500
```

```bash
python scripts/collect_dart_filings.py
```

## 구조

- `config/settings.py` — 유니버스 크기, 호라이즌 등. **티커 목록을 박지 않는다**(생존편향)
- `src/data_loader/panels.py` — 시장 데이터 묶음. 분석은 여기서 시작한다
- `src/strategy/` — 전략 인터페이스. 전략은 "무엇을 얼마나 들지"만 정하고 체결·비용은 엔진 몫
- `src/portfolio/portfolio_engine.py` — t+1 체결 강제, 평가금액 기준 주문 크기
- `src/costs/cost_model.py` — 수수료·슬리피지·시장충격(Y × 변동성 × √참여율)·증권거래세
- `src/research/` — IC, 롱온리 분위, 검정력
- `scripts/run_*.py` — 각 파일 docstring에 그 검정의 사전 등록이 적혀 있다

`src/data_loader/{krx_loader,krx_panel}.py`, `src/features/multi_source.py`는 각각
한두 곳에서만 참조되는 잔존 모듈이다. 건드릴 일이 있으면 정리해도 된다.
